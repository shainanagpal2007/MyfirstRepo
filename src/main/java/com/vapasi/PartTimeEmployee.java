package com.vapasi;

public class PartTimeEmployee extends CompEmployee{
    PartTimeEmployee(long id, String firstName, String lastName, int salary) {
        super(id, firstName, lastName, salary);
    }
}
